<template>
  <li class="card variant-3">
    <a href="" class="card-link">
      <div class="left">
        <img src="" alt="" class="thumb small" />
      </div>
      <div class="right">
        <h3 class="title"></h3>
        <p class="description"></p>
      </div>
    </a>
  </li>
</template>

<style>
  li { list-style: none; }

  .card {
  padding: 0.5rem 0.6rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  background: rgba(255,255,255,0.02);
  backdrop-filter: blur(4px);
  border-radius: 8px;
  transition: transform 200ms cubic-bezier(.2,.9,.3,1), box-shadow 200ms;
  animation: slideInLeft 420ms cubic-bezier(.2,.9,.3,1) both;
  }

  .thumb.small {
    width: 48px;
    height: 48px;
    object-fit: cover;
    border-radius: 0.375rem;
  }

  .title { margin: 0; font-size: 0.92rem; color: #111827; }
  .description { margin: 0; font-size: 0.82rem; color: #6b7280; }

  a { display: flex; gap: 0.6rem; align-items: center; text-decoration: none; color: inherit; }

  .card:hover { transform: translateX(6px); box-shadow: 0 6px 22px rgba(2,6,23,0.06); }

  /* Slide-in animation from left */
  @keyframes slideInLeft {
    from { opacity: 0; transform: translateX(-12px); }
    to { opacity: 1; transform: translateX(0); }
  }
</style>

<style is:global>
  /* Variant3: estilo compacto — reglas globales que afectan el input y resultados */
  #search-input,
  .astro-search-component input[type="search"] {
    width: 100%;
    padding: 0.45rem 0.9rem;
    border: 1px solid #cbd5e1; /* soft blue-gray */
    border-radius: 0.375rem;
    font-size: 0.95rem;
    outline: none;
  }

  #search-input:focus, .astro-search-component input[type="search"]:focus {
    box-shadow: 0 1px 6px rgba(14,165,233,0.12);
    border-color: #38bdf8;
  }

  #search-results {
    margin-top: 0.4rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  /* Compact list appearance */
  .astro-search-component .card { border-radius: 0.375rem; background: transparent; }
  .astro-search-component .card .title { font-weight: 600; }
</style>
